{
  "name": "driftschema-mongo",
  "version": "0.1.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "example": "tsx examples/basic-usage.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "author": "dimis1995",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dimis1995/driftschema.git",
    "directory": "packages/mongo"
  },
  "description": "MongoDB storage engine for driftschema — persists records and field definitions to MongoDB collections.",
  "dependencies": {
    "driftschema": "^0.1.0",
    "mongodb": "^7.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "mongodb-memory-server": "^11.2.0"
  }
}
